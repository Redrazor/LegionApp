node_modules/
dist/
.env
.env.local
public/images/units/
public/images/portraits/
public/images/upgrades/
public/images/commands/
public/images/battle/
public/images/products/
# Compressed WebP for the Firebase image CDN (regenerated by `npm run images:compress`)
/images-compressed/
server/db/legion.db
server/db/legion.db-shm
server/db/legion.db-wal
coverage/
public/data/backups/*.json
/tmp/

.playwright-mcp/

# Firebase CLI deploy cache (regenerated on every `firebase deploy`)
.firebase/

# Generated portrait QA contact sheet (regenerated by `npm run portraits:validate`)
portrait-validation.html

# Local scratch (hand-export staging, not part of the app)
Extra portraits/

# AMG print-and-play PnP sources + extracted card staging (2.0 image re-source).
# Provenance is tracked in card_list_origin.md; the binaries are not committed.
scraper/amg-pdfs/
scraper/amg-cards/
# Intermediate matching/validation artifacts (regenerated per faction batch).
scraper/amg-card-map.json
scraper/amg-approvals.json
image-validation.html

# Outward-facing marketing drafts are kept out of the public repo
community/
