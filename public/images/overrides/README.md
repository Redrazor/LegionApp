# Card image overrides

Drop a high-resolution unit card scan here named `<slug>.webp` (matching the
unit's `slug` in `public/data/units.json`). The data pipeline copies it into
`public/images/units/` and never overwrites it, so it survives re-scrapes.

Use this to upgrade any low-resolution card to a better scan.
